using Microsoft.AspNetCore.Identity;

namespace QuantFloww.Infrastructure.Identity
{
    public class ApplicationUser : IdentityUser
    {
        // Add custom properties here if needed in the future (e.g. FirstName, LastName)
    }
}
